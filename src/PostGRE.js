export const create = (question, answer) => {
	console.log("question: " + question);
        console.log("answer: " + answer);
}

export const retrieve = () => {
        console.log("question: " + question);
        console.log("answer: " + answer);
}

export const update = (index, question, answer) => {
        console.log("question: " + question);
        console.log("answer: " + answer);
}

export const delete = (index) => {
        console.log(index);
}

